import 'package:flutter/material.dart';

class RefuelScreen extends StatelessWidget {
  const RefuelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Refuel Screen'),
    );
  }
}
